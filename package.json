{
  "name": "atenea",
  "version": "0.0.0",
  "private": true,
  "description": "ATENEA — self-improving coding agent for team repositories",
  "license": "see LICENSE",
  "packageManager": "pnpm@11.3.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "atenea": "pnpm --filter @atenea/cli start"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.10.0"
  }
}
